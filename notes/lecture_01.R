library(tidyverse)
library(here)

# r works as a calculator
7*8*3-2

# <- assignment

object_one <- 3*4

this_is_a_really_really_really_long_name <- 8
This_is_a_really_really_really_long_name

object_two <- c(86, 11, 52, 8)
object_two*2

object_three <- c(object_two, 67)
object_three <- c(object_two, "six")

sum(object_two)

object_four <- object_one + 7
object_one <- 6
